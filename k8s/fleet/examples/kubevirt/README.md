# KubeVirt examples

The `fedora-vm.yaml` file defines an opt-in Fedora virtual machine and a LoadBalancer Service.

The example contains a public SSH key and an internal image URL. Replace both values before you apply it elsewhere.

Argo CD does not discover this directory.
