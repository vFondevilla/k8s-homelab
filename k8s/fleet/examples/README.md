# Fleet examples

Examples are non-reconciled references and experiments. They must remain
outside every automatic Argo discovery pattern and must not contain generated
kubeconfigs or private credentials.
