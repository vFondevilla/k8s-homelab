apiVersion: v1
kind: Service
metadata:
  name: zigbee2mqtt
spec:
  type: LoadBalancer
  selector:
    app: zigbee2mqtt
  ports:
    - name: http
      port: 8080
      targetPort: web
      protocol: TCP
