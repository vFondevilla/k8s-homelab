apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hwpo-flex

labels:
  - pairs:
      app.kubernetes.io/part-of: hwpo-flex
    includeSelectors: true

resources:
  - namespace.yaml
  - pv.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
