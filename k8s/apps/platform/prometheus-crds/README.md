# Prometheus CRDs platform component

Prometheus Operator CRDs are a management-cluster observability capability.
The component retains a separate child Application so CRD lifecycle and
ordering remain independent from the Prometheus stack.
