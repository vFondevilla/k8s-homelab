apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mosquitto

resources:
  - namespace.yaml
  - pv.yaml
  - pvc.yaml
  - configmap.yaml
  - serviceaccount.yaml
  - service.yaml
  - deployment.yaml

images:
  - name: eclipse-mosquitto
    newTag: 1.6.15
