apiVersion: v1
kind: Service
metadata:
  name: zigbee2mqtt
  annotations:
    external-dns.alpha.kubernetes.io/hostname: zigbee2mqtt.fondevilla.io
spec:
  type: LoadBalancer
  selector:
    app: zigbee2mqtt
  ports:
    - name: http
      port: 8080
      targetPort: web
      protocol: TCP
