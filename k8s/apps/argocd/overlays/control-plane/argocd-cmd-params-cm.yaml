apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
  namespace: argocd
data:
  server.insecure: "true"
  reposerver.exec.timeout: "300"
  # Allow per-ApplicationSet create-update transition guards.
  applicationsetcontroller.enable.policy.override: "true"
