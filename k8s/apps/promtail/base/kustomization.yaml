apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: promtail

labels:
  - pairs:
      app.kubernetes.io/part-of: promtail
    includeSelectors: true

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - configmap.yaml
  - daemonset.yaml
  - service.yaml
